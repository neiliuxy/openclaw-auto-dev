# OpenClaw Auto Dev 项目配置
repo: neiliuxy/openclaw-auto-dev

llm:
  provider: dashscope
  model: qwen3.5-plus
  api_key: ${DASHSCOPE_API_KEY}

build_cmd: make
test_cmd: make test

languages:
  - cpp
  - python
