你是 Agent-Architect。请为 Issue #30 撰写需求规格说明书。

**Issue 信息：**
- 标题：feat: 添加 C++ 字符串处理工具 string_utils
- 描述：## 背景

需要一个常用的字符串处理工具库，方便其他程序调用。

## 工作内容

1. 新建 `src/string_utils.cpp` 和 `src/string_utils.h`
2. 实现以下功能：
   - `trim()` - 去除首尾空白字符
   - `split()` - 按分隔符分割字符串
   - `join()` - 将字符串数组合并
   - `to_lower()` / `to_upper()` - 大小写转换
   - `starts_with()` / `ends_with()` - 前缀后缀判断
   - `replace()` - 字符串替换
   - `is_numeric()` - 判断是否为数字
3. 提供头文件供其他模块使用
4. 编写单元测试

## 验收标准

- [ ] 所有函数测试通过
- [ ] 头文件设计合理，可复用
- [ ] 编译通过无警告

## 涉及技术

- C++ (string, vector)
- 单元测试

**任务：**
1. 分析需求，拆解功能点（每条可独立验证）
2. 设计技术方案（目录结构、核心模块）
3. 制定验收标准（每条功能点对应可测试判定条件）
4. 将完整 SPEC.md 输出到：/home/admin/.openclaw/workspace/openclaw-auto-dev/SPEC.md

**要求：**
- 验收标准必须可自动化测试或明确判定通过/失败
- 如需求模糊，在 SPEC.md 中注明需人工确认
- 技术方案需符合 C++ 项目风格

直接生成 /home/admin/.openclaw/workspace/openclaw-auto-dev/SPEC.md 文件。
