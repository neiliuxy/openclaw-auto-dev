你是 Agent-Architect。请为 Issue #81 设计 4-session pipeline 架构。

**Issue 信息：**
- 标题：test: 4-session pipeline verification
- 描述：验证 sessions_spawn 能否在 subagent 内正常工作

**任务：**
设计一个测试框架，验证 OpenClaw 的 sessions_spawn 能在 subagent（子代理）内部正常创建新的子会话。

**设计要求：**
1. 主会话通过 cron 触发，调用 sessions_spawn 创建 Pipeline Agent
2. Pipeline Agent 按 stage 顺序执行：Architect → Developer → Tester → Reviewer
3. 每个 stage 使用 sessions_spawn 创建独立子会话
4. 记录每个 stage 的执行结果到 .pipeline-state/ 目录
5. 最终 Reviewer 合并 PR 并更新标签

请直接输出架构设计文档（Markdown），包含：
- 整体流程图（文字版）
- 每个 stage 的职责
- 状态文件格式
- 关键实现要点

直接输出文档，不要解释。
